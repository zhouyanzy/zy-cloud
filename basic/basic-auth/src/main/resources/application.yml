server:
  port: 8790
spring:
  application:
    name: basic-auth
  redis:
    database: 1
    host: 47.101.151.43
    port: 6379
    password: zy1217
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

eureka:
  client:
    serviceUrl:
      defaultZone: http://zhouy.top:8760/eureka/

security:
  basic:
    enabled: true

management:
  endpoint:
    hystrix:
      stream:
        enabled: true
    health:
      show-details: ALWAYS
  endpoints:
    web:
      exposure:
        include: '*'